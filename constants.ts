export const MIN_RADIUS = 0.1;
export const MAX_RADIUS = 1.8;
export const DEFAULT_RADIUS = 1.0;
export const GRID_SIZE = 120; // Resolution for the contour plot

export const SAMPLE_QUESTIONS = [
  "What happens to time inside the horizon?",
  "Why is it called a 'flip'?",
  "Can light escape from r < 1?",
];
